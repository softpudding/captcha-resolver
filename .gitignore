# Virtual environment
.venv/

# Run output: per-run screenshots, transcript.jsonl, result.json (large/local)
runs/

# Eval run records + per-run audit trails (the tasks/ fixtures ARE kept — they're the dataset)
eval/results/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# OS
.DS_Store
